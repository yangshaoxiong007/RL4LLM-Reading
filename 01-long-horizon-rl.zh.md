<img width="100%" src="https://capsule-render.vercel.app/api?type=waving&color=0:0F2027,55:5B8CFF,100:9B6BFF&height=140&section=header&text=01%20%C2%B7%20%E9%95%BF%E7%A8%8B%E4%BB%BB%E5%8A1%20%2B%20RL&fontSize=30&fontColor=ffffff&fontAlignY=50&fontAlign=50&animation=fadeIn"/>

<p align="center"><sub>🇨🇳 中文版 &nbsp;·&nbsp; <a href="./01-long-horizon-rl.md">🇬🇧 English</a> &nbsp;·&nbsp; 交叉引用 <a href="./02-credit-assignment-rl.zh.md">02 信用分配</a> · <a href="./03-process-reward-search.zh.md">03 过程奖励</a></sub></p>

## 1 · 为什么重要

对 LLM 智能体而言,"长程任务"指那些**决定成败的动作**与**最终结果信号**之间隔着成百上千个 token、乃至多次工具/搜索往返的任务。它必然带来三个失败模式:

1. **稀疏与延迟奖励**。答案或证据判定只在轨迹末尾到达;在长度为 $H$ 的轨迹上,任何早期决策的梯度信噪比只有 $O(1/H)$。
2. **步级信用稀释**。即便结果正确,梯度也无法分辨是哪一步导致的。纯结果 RL 隐含一个**均匀信用先验**——对推理是错的(一步走错全盘皆输),对多步搜索更是错的。
3. **训练/推理长度错配 & 奖励黑客**。在长 rollout 上优化末端结果会奖励"以长度当策略",并催生捷径(LLDS:在"容易 token"上加概率而非"关键 token";ASTER:退化为大量内部推理、极少工具调用)。

这条线索的本质是**时间维度的信用归因**:如何让奖励在长 LLM rollout 中忠实传播。

## 2 · 形式化

把 LLM 视为 token 上的策略 $\pi_\theta$;horizon $H$ 的推理/智能体 rollout 是轨迹 $\tau=(s_0,a_0,\dots,s_H,a_H)$,"动作"在粗粒度上是一个推理步或工具调用。RL 目标是标准的代理目标(PPO 式留一 / GRPO 式组基线):

$$J(\theta)=\mathbb{E}_{\tau\sim\pi_\theta}\Big[\,\sum_{t=0}^{H} R(s_t,a_t) - \beta\,\mathrm{KL}(\pi_\theta\|\pi_{\mathrm{ref}})\Big]$$

**结果奖励**把 $R=\sum_t r_t$ 塌缩为末端 $R(\tau)=r_H$(稀疏、延迟)。长程 RL 实际上操纵三个量:

- **基线方差**。GRPO 用**组相对优势**替代学习版 критic $V_\phi$:$\hat A_i=(r_i-\mathrm{mean})/\mathrm{std}$。去掉一个在 $H\!\gg\!100$ 步上极难拟合的噪声价值头——这是长程 LLM RL 的关键工程选择(见 §4)。
- **有效 horizon**。只有接近 $s_H$ 的状态/动作获得有信息量的优势,早期步骤**不可归因**。过程奖励(见 doc 03)是标准解药。
- **KL 锚定**。$\beta$ 控制相对 SFT 先验的漂移;$\beta$ 太小→奖励黑客/退行长 rollout,太大→不探索。最新趋势(Dr. GRPO、DAPO)是把 **KL/长度控制与优势估计器本身解耦**。

## 3 · 方法谱系

| 流派 | 核心思想 | 长程相关性 | 代表工作 |
|---|---|---|---|
| **结果 RL + 组基线** | 去掉 critic,跨 $G$ 样本做基线 | 去掉噪声长程价值估计 | GRPO(arXiv:2402.03300✓);DeepSeek-R1(arXiv:2501.12948✓) |
| **结果 RL + 解耦控制** | 优势估计器与 KL/长度惩罚分离 | 抑制长度黑客同时保留探索 | Dr. GRPO(arXiv:2503.20783✓);DAPO(arXiv:2503.14476✓) |
| **可验证奖励 RL(RLVR/RFT)** | 奖励=可执行 verifier | 让末端奖励即使长 rollout 也廉价可信 | R1 冷启动;ReFT(arXiv:2401.08967✓) |
| **步级/过程监督 RL** | 用 PRM 奖励中间步 | **正面强攻 horizon**——密集且可归因 | Let's Verify(arXiv:2305.20050✓);Math-Shepherd(arXiv:2312.08935✓) → doc 03 |
| **崩溃诊断与正则化** | 分析并修正长 rollout 上的策略退化 | 直接针对长程训练病态 | LLDS(arXiv:2512.04220✓);ASTER(arXiv:2602.01204✓) |
| **工具/搜索一体化 RL** | 动作含检索/工具调用,只奖最终答案 | 长程 *与外部状态交织* | Search-R1/R1-Searcher/ReSearch → doc 03 |

## 4 · 论文剖析

### GRPO — *DeepSeekMath*(arXiv:2402.03300✓)
**贡献。** 组相对策略优化:每 prompt 采 $G$ 条,优势为标准化组奖励,**组内共用一个基线、无学习版 $V_\phi$**。这是长程 LLM RL 最重要的工程选择——在数百 token 的思考 rollout 上拟合一台价值头,是方差与训练病态的主要来源,去掉它既更省又更稳。
**理论裂缝。** 组基线仅当 $G$ 条样本给定 prompt 可交换时无偏;对数学可验证推理近似成立,但对**多智能体/角色异构** rollout **不**成立:planner 与 searcher 产出同一结果的回报是不对称的。这正是信用分配(doc 02)重新切入 GRPO 的接缝。
**局限。** 仍是纯结果;horizon 问题靠降方差缓解,**未被解决**。

### DeepSeek-R1(arXiv:2501.12948✓)
**贡献。** 纯 RLVR(无 SFT 推理数据)即可涌现长而有产出的 CoT,含自我反思/"aha",用的是 GRPO+可验证奖励。说明**长度先验不必教**:奖励可验证且 KL 锚定得当时,RL 能自然长出 horizon。
**关键的隐含论断。** "涌现"的长程能力其实*被解释*为:verifier 提供了足够密集的末端信号,且 SFT 先验里已含潜在技能;RL 只是**选择并延展**它们。抽掉 SFT 先验质量,"aha 时刻"不会出现——许多复现失败是栽在先验、而非 RL。
**开放伤口。** 长度泛化(推广到比训练更难更长的题)仍不可靠;不可验证子部分上的奖励黑客持续存在。

### LLDS:GRPO 崩溃(arXiv:2512.04220✓)
**贡献。** 系统诊断搜索智能体上 GRPO 的策略崩溃:**Lazy Likelihood Displacement(LLD)**——模型倾向在"容易 token"上加概率、而非"关键 token";演化分三阶段(停滞→衰退→崩溃)。根因是组相对优势估计**在长搜索轨迹上不稳定**。LLDS 正则化可挽回 ~45%。
这是对 doc01 §2"GRPO 选组基线"那条工程捷径的**迟来的账单**:去掉 critic 的代价,在长搜索 horizon 上以"崩溃"形式爆发。它不是孤立 bug,而是"长程 + 纯结果 RL"结构性脆弱的实证。

### ASTER:交互崩溃(arXiv:2602.01204✓)
**贡献。** 指出另一类崩溃:RL 中内部推理的奖励更密集(每步可评),工具调用反馈稀疏且延迟,于是模型退化为**大量内部推理 + 极少工具调用**。4K 交互密集冷启动可缓解但缺通用性。
**与 LLDS 的联合读法。** LLDS 是"长度/概率位移"崩溃,ASTER 是"工具 vs. 推理"的信号密度崩溃——两者都指向同一根源:**长程上、靠末端结果的 RL 必然把策略推向信号最密(但未必最有效)的行为模式**。这正是过程奖励(doc 03)必须介入的地方。

### Dr. GRPO / DAPO(arXiv:2503.20783✓ / 2503.14476✓)
**贡献。** 指出 GRPO 的标准化与 KL/长度惩罚是**耦合**的:按组 std 标准化使有效步长依赖奖励尺度,长度控制行为不连贯。解耦(Dr. GRPO 去 std 归一;DAPO 重构 clip/长度惩罚)稳住长程训练,并在小规模复现更多 R1 行为。
这是**对估计器的修正**,不是新范式——重要之处在于它们揭示:许多已发表的"涌现"对归一化/idiosyncrasy 高度敏感。跨组/跨 horizon 的可复现性才是真指标。

## 5 · 批判——领域在何处过度声称

- **"可验证奖励解决了稀疏"**——只对验证廉价且精确的任务(数学、代码、事实检索)成立。对开放式深度研究,验证本身是未解问题;那里的结果 RL 承受全部稀疏性。这正是过程奖励(doc 03)的意义。
- **horizon 被默认成长度,而非深度。** "长程"与"很多 token"被混用。5000 token 的发散是长 horizon 但决策前沿浅;信用问题关乎**因果依赖的深度**,token 数量不度量它。文献对此语焉不详。
- **KL 锚定是创可贴。** $\beta$ 在做本应由**忠实信用归因**完成的工作。一旦移除过程结构,就只能靠 $\beta$ 防漂移——这封死了长程能力能走多远。

## 6 · 开放问题(→ 作者的研究线)

1. **无外部 PRM 的忠实步级信用。** 异构分组结构(doc 02)能否*替代*手工塑形的过程奖励?
2. **搜索即长程 MDP。** 当 horizon 是*搜索迭代*(而不仅是 token),状态空间是不断增长的证据上下文;价值估计与奖励塑形基本是开放的。
3. **研究、而非数学/代码的可验证奖励。** 长程*深度研究* RL 的瓶颈在 verifier、不在算法。
4. **多智能体 × 长 horizon 的交互。** GRPO 的可交换性假设在角色异构下失效;组合 docs 02 ⇆ 01 ⇆ 03 是未解核心。

## 参考文献(经 § 逐条核验)

- GRPO / DeepSeekMath — **arXiv:2402.03300** ✓
- DeepSeek-R1 — **arXiv:2501.12948** ✓
- LLDS(GRPO 崩溃) — **arXiv:2512.04220** ✓
- ASTER(交互崩溃) — **arXiv:2602.01204** ✓
- Dr. GRPO — **arXiv:2503.20783** ✓("Understanding R1-Zero-Like Training")
- DAPO — **arXiv:2503.14476** ✓
- ReFT — **arXiv:2401.08967** ✓
- Let's Verify Step by Step — **arXiv:2305.20050** ✓(交叉引用 doc 03)
- Math-Shepherd — **arXiv:2312.08935** ✓(交叉引用 doc 03)

<p align="center"><sub>01 / 03 · 长程 RL · 交叉引用 02 信用分配、03 过程奖励</sub></p>
